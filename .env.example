# Database
DATABASE_URL=postgres://clawvet:clawvet@localhost:5432/clawvet

# Redis (for BullMQ scan queue)
REDIS_URL=redis://localhost:6379

# Anthropic API key (for semantic analysis pass)
ANTHROPIC_API_KEY=sk-ant-...

# GitHub OAuth (for user auth)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost:3001/api/v1/auth/github/callback

# JWT secret for session tokens (generate a random 64-char string for production)
JWT_SECRET=clawvet-dev-secret-change-me

# Web app URL (for OAuth redirect)
WEB_URL=http://localhost:3000

# API
PORT=3001
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT_MAX=100

# Next.js web app
API_URL=http://localhost:3001
